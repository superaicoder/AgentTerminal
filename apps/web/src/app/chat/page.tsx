"use client";

export default function ChatIndexPage() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "var(--muted)",
        fontSize: "0.875rem",
      }}
    >
      Select a conversation or create a new one
    </div>
  );
}
